import React from 'react';

interface Props {
  // define props here
}

const CourseGoal: React.FC<Props> = ({  }) => {
  return (
    <div>
      // component content
    </div>
  );
}

export default ComponentName;